# -- Configurazione --
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  # Hotspot di fallback se ethernet non funziona
  ap:
    ssid: "VMC Sabiana Fallback"
    password: !secret fallback_password
